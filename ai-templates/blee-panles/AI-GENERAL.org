#+title: AI Agents Guidance for bisos.newPkg

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

* Table of Contents     :TOC:
- [[#general-this-repo-is-part-of-bisos-pip----common-structure-of-repos-in-the-bisos-pip-organization][General: This Repo is Part of bisos-pip -- Common Structure of Repos in the bisos-pip Organization]]
- [[#general-python-programming-and-testing][General: Python Programming and Testing]]
- [[#git-commits-and-git-pushes][Git Commits and Git Pushes]]
- [[#only-one-of-the-latest-2-release-debian-and-only-one-of-the-latest-2-releases-of-any-given-app][Only one of the Latest 2 Release Debian and only one of the Latest 2 Releases of any given App]]
- [[#general-this-repo-is-layered-on-top-of-pycs-framework-bisosb-and-bisospycs][General: This Repo is Layered on Top of PyCS-Framework (bisos.b and bisos.pycs)]]
- [[#general-commands][General: Commands]]
- [[#general-writing-cs-commands][General: Writing CS commands]]
- [[#concept-of-bisos-capabilities][Concept of BISOS Capabilities]]
  - [[#web-virtual-domain-services-wvds-pattern][Web Virtual Domain Services (wvds) Pattern]]
  - [[#cbs-triggers-hierarchy-for-rawbisos][CBS Triggers Hierarchy for rawBisos]]

* General: This Repo is Part of bisos-pip -- Common Structure of Repos in the bisos-pip Organization

Read

/bisos/git/bxRepos/bisos-pip/.github/profile/readme.org

when needed.

* General: Python Programming and Testing

For any new project or module or file, use type hinting.
For exisiting code, if type hinting is not used, don't add any hints.

This  project does not have a CI/CD or liniting tools.

See Specific for smoke tests.

The regression testing has the form of RawBisos re-construction.
All of bisos-pip packages are used in that context.

* Git Commits and Git Pushes

I don't want Claude to suggest me to prompt me:  "commit this" or "push this".
I want to do all the commits and pushes myself.
Instead just suggest that I myself can commit.

* Only one of the Latest 2 Release Debian and only one of the Latest 2 Releases of any given App

None of what we do needs to be multi-platform. We only care about our own
ByStar Digital Ecosystem.

Windows compatibility is never required.
Backwards compatibility is limited only to the two latest releases.

* General: This Repo is Layered on Top of PyCS-Framework (bisos.b and bisos.pycs)

PyCS-Framework is documented in:

@/bisos/panels/bisos-core/PyCsFwrk/_nodeBase_/fullUsagePanel-en.org
--- the framework's own usage panel; read for the semantics of =cs.Cmnd=,
=g_csMain=, =cs.examples.*=, parameter / argument specs, and RO (remote
operations).

The PyCS-Framework package is at: /bisos/git/bxRepos/bisos-pip/pycs
--- the framework source (=bisos.csPlayer= and related); consult when you need
how the CS player, =csuList= imports, or seeding hooks actually work.

PyCS-Framework augments the bisos.b package which is at /bisos/git/bxRepos/bisos-pip/b
--- =bisos.b= provides the =cs=, =b_io=, and =b.op= APIs this repo imports.
Its Blee-Panel is @/bisos/git/bxRepos/bisos-pip/b/py3/panels/bisos.b/_nodeBase_/fullUsagePanel-en.org

When descending into either repo, follow its own =AI-AGENTS.org= (if present)
the same way this file points up to the org-wide readme.

* General: Commands

All commands assume you are in =py3/=. For build, install, and publish, see
*bisos-pip Build System* above.

- *Lint / type-check* (paths from the in-file "Workbench" headers): the venv
  interpreter is =/bisos/venv/py3/bisos3/bin/python=; linters live in
  =/bisos/pipx/bin/= (=pyflakes=, =flake8=, =pylint=, =pycodestyle=). =mypy= is
  used (a =.mypy_cache/= for py3.11 is present).

- *Smoke test* (no test suite exists): run a seed's example menu, e.g.
  =bin/addCmnds-helloWorld.pcs= (prints the example menu) or
  =bin/addCmnds-helloWorld.pcs -i helloWorld= (runs the =helloWorld= command).

There is no unit-test framework in this repo. Verification is done by running
the =.cs=/=.pcs= executables and inspecting their output --- the expected output
is captured inline in each command via =self.captureRunStr(...)=.

Some execution examples are at:
@/bisos/git/bxRepos/bisos-pip/gitist/py3/panels/bisos.gitist/_nodeBase_/fullUsagePanel-en.org

* General: Writing CS commands

Commands are classes subclassing =cs.Cmnd= (from =bisos.b import cs=). Each
declares =cmndParamsMandatory=, =cmndParamsOptional=, =cmndArgsLen=, and a
=cmnd()= method that does the work. The gitist commands live in
=py3/bisos/gitist/gitist_csu.py= --- read =whichReactFramework= and
=placeHolder= there as templates.

The class header (the =class <Name>(cs.Cmnd):= line and the =cmnd()= signature)
is generated from a =####+BEGIN: b:py3:cs:cmnd/classHead ...= dblock --- do not
hand-edit between the =BEGIN=/=END= markers. Author only the body *after* the
=####+END:= marker:

- =cmndParamsMandatory= / =cmndParamsOptional= :: lists of parameter names the
  command accepts (declared on the =classHead= dblock as =:parsMand= / =:parsOpt=).
- =cmndArgsLen= :: =({'Min': N, 'Max': M})= positional-argument bounds. When the
  command takes args, also define a =cmndArgsSpec()= method (its own
  =b:py3:cs:method/args= dblock) using =cs.CmndArgsSpecDict().argsDictAdd(...)=,
  and read them in =cmnd()= with =self.cmndArgsGet(...)=.
- =cmnd(self, rtInv, cmndOutcome, ...)= :: the implementation. Start by calling
  =self.cmndDocStr(...)= for the org description, optionally
  =self.captureRunStr(...)= to capture expected example output (this *is* the
  test --- see *Commands* above), do the work, and return
  =cmndOutcome.set(opError=b.OpError.Success, opResults=...)=.

Register the command in the seed's example menu (=examples_csu= /
=examples_seed= in the same file) so it shows up in the plant's example output.

* Concept of BISOS Capabilities

 BISOS Capabilities allow for addition of features to BISOS.

 Read

 @/bisos/panels/bisos-core/modelAndTerminology/_nodeBase_/fullUsagePanel-en.org

 Which describes the Model and Teminology of BISOS and ByStar.

Also Read

 @/bisos/panels/bisos-core/bisos-pip/bisos.capability/_nodeBase_/fullUsagePanel-en.org

 Which describes some details of how BISOS-capabilities  are implemented.

 Exisiting web virtual domain services are at the following directory.

 @/bisos/asc/web/bin/

** Web Virtual Domain Services (wvds) Pattern

This is a common category of web services in which a service run at a port and then
is exposed at a virtual domain. In addition to the ByStar domain specification is general
the web service is also exposed at localhost.

See

@/etc/hosts

As a wvds, consider airflow in the context of airflow.here of /etc/hosts as an example:

@/bisos/asc/web/bin/airflow-sbom.pcs                 :: Installs airflow and its dependencies
@/bisos/asc/web/bin/airflow-webserver-sysd.pcs       :: Creates systemd entry/ies for airflow
@/bisos/asc/web/bin/airflow-here-wvds-nginx.pcs      :: NOTYET, creates virdom entry for here and airflow
@/bisos/asc/web/bin/airflow-here-dns.pcs             :: NOTYET, for here, run dblock on etc/hosts
@/bisos/asc/web/bin/airflow-cbs-wvds-np-sysd.pcs     :: Does all of the above

** CBS Triggers Hierarchy for rawBisos

@ /bisos/git/bxRepos/bxObjects/bro_rawBisos/bro_rawBisosPlatform
@ /bisos/git/bxRepos/bxObjects/bro_rawBisos/bro_rawBisosPals
@ /bisos/git/bxRepos/bxObjects/bro_rawBisos/bro_rawBisos_pbsi-here
